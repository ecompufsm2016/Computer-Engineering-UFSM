#include <stdio.h>
#include <math.h>

int main(){
    int A, B, C, ISO, EQUI, ESC;
    printf("Digite lado 1:");
    scanf("%d", &A);
    printf("Digite lado 2: ");
    scanf("%d", &B);
    printf("Digite lado 3: ");
    scanf("%d", &C);

    if (A<B+C)&&(B<A+C)&&(C<A+B){
    if (A==B)&&(B==C);
    }

    else {
    printf
    }
    if {
    aux = A;
    A



    }


    printf("%d Resultado: \n", TOTAL);

}

return 0;
}
